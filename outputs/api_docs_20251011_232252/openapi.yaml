info:
  description: API Documentation Generated
  title: API Documentation
  version: 1.0.0
openapi: 3.0.0
paths:
  /users:
    post:
      responses:
        '200':
          description: Successful response
      summary: Registers a new user in the system. Requires administrative privileges.
  /users/{user_id}:
    get:
      responses:
        '200':
          description: Successful response
      summary: Retrieves the profile details for a specific user. Users can retrieve
        their own profile; administrators can retrieve any profile.
servers:
- description: Development server
  url: http://localhost:3000
